# data/

Deterministic, seeded generator for the **Northgate Provisions Co.** dataset, plus the
committed CLEAN and RAW DIRTY JSON files.

- **Clean curated data** — used by Genie (practical 1) and as a Lakebase fallback.
- **Raw dirty JSON** — JSON-in-volume files for the SDP lab (practical 2), with
  deliberately seeded data-quality issues.

> _Placeholder — the schema and the precise list of seeded DQ issues are documented here
> by the `data` teammate (Task #2)._
